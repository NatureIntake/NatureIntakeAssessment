// import Cookies from "js-cookie";

// function handleCookie() {
//   if (props.Session) {
//     let id = props.session.user.id;
//     id = JSON.stringify(id);
//     Cookies.set("userId", id);
//   } else Cookies.set("userId", "");
// }
